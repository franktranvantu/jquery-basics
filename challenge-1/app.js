// CSS is being used to hide three items on the index.html page (two <li> elements and a <div> element).
// Use jQuery's :hidden pseudo selector and the show() method to display the hidden <li> elements, while leaving the <div> element hidden.