// Using the on() method, attach an event handler to handle click events
// on the ul element with the class of .student-list.
// Be sure to pass in the event object to the callback function.
$('.student-list').on('click', function (event) {
  
})